#include <stdio.h>

int main() {
    int size;
    printf("veuiller tappez size de tableau");
    scanf("%d"size);
    int tableau[size];
    for(int i=0;i<size;i++){
        printf("ecrire %i elemant de tableau:", i+1);
        scanf("%d",&tableau[i]);
    {
        grand = tableau[0]
        if(tableau[i]>grand){
            grand = tableau[i];
        }
    }
    
    return 0;
}